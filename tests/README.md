# Test Layout

Tests will be divided into pure unit tests, local integration tests, sanitized fixture
browser journeys, and separately authorized manual pilots. CI must never open the live
Wellfound site or load personal runtime data.
